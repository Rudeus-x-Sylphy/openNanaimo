# Changelog

## 2026-09-21 — Nanaimo adapter release

### 功能

- 新增任务、社交、交易、竞技、娱乐、公寓及对应 managed 数据层功能。
- GUI 改为一套完整 adapter：`Nanaimo.Adapter.exe` 统一承载角色、社交、任务、商城、公寓、交易、竞技和娱乐；`nanaimo_gameplay_bridge.exe` 作为内部地宫桥接组件。
- 删除 GUI 的第二套独立 adapter 按钮和控制脚本；所有功能统一进入完整 adapter。
- 绿色 **“一键进入 Nanaimo”** 成为主要启动入口；蓝色按钮只用于单独启动或停止完整 adapter。
- 新增完整 adapter 构建、manifest、启动等待、优雅停止和隔离 self-test。

### 蓝陌贡献

以下业务能力由 **蓝陌** 贡献：

- 任务和任务卷轴；
- 好友、师徒、情侣；
- 组队、玩家交易、卡片交易所；
- 天空竞技场与娱乐房间；
- 公寓和愿望清单；
- managed 角色、库存、商城、宠物及数据持久层；
- 与原生地宫桥接所需的业务状态模型。

普通怪物卡片掉率等数值策略调整属于本版本的例行修改，不计入蓝陌贡献。

### 修复

#### GUI 配置应用

- “一键进入 Nanaimo”在启动前保存当前 GUI 配置，并将最新资料注册到适配器。
- 已有人物档案可更新 HP/MP、货币、外观、装备宠物、技能及 GUI 管理的衣物、宠物、游戏道具、卡片和家具数据。
- 重新应用 GUI 配置时保留人物等级、经验和上次下线位置；新人物仍从游戏原始创建与新手引导入口开始。
- 客户端校验同时识别已审查基线与家具崩溃修复版本，其他大小或哈希仍拒绝启动。

#### 宠物成长

- 原生地宫状态交换新增宠物等级与经验。
- 正向地宫结算在同一事务中推进装备宠物。
- 重复提交不重复增加宠物经验。
- `CF72` 写入真实宠物等级。
- `CF88` 写入宠物升级标志、当前等级、阶段等级上限和经验，并重算校验和。
- `C44C`、`C379`、`CF72`、`CF88` 使用同一持久宠物状态。

#### Hans 商城

- 补齐 `gi._D6` 中遗漏的 18 个 Hans 定价商品。
- 商城按目录币种分别扣除 Hans 或 NaNa。
- 支持客户端使用的 `C431` 付款模式 `0/2/3/4`。
- 保存或实例分配失败时回滚余额、数量和 identity。

#### 一代／二代整容券

- 补齐 `C46F/C470 -> C475/C476 -> C430 -> C3D1 -> C3D2 -> C47F` 链路。
- 修正 C3D1 尾部字段偏移为 payload `+60/+62/+64/+66`。
- 按准确 C430 identity 消耗整容券。
- 一代券限定脸型 `10100004..10100008`，二代券限定 `10100009..10100013`。
- 成功后持久化并即时刷新；失败完整回滚。

#### 复活

- 未发现需要修改的确定性 adapter Bug。
- 已复核购买、激活、持久计数、`C355/CF71` 载体、death latch、`CF95 -> CF96/CF84/CF72` 顺序和重复请求幂等。
- 原客户端复活后的可见位置与操作恢复仍保留为试玩验收项。

### 客户端 bug 修复指引

- 家具选择崩溃 bug：在基线 `game.exe` 的 `VA 0x0041235F`，将跳转字节 `E9 9C C3 1B 00` 替换为 `E9 7C CC 1B 00`，改用原生 Index getter。
- 拉米诺斯村地宫 7 进入与资源加载 bug：按顺序准备 C355 前置进度、P03 道路 pack、SSTG 别名、限定资源映射和 PON 同家族回退。
- 客户端补丁的基线、步骤和验收项见 [完整 adapter 与客户端补丁指引](docs/完整适配器与客户端补丁指引.md)。

### 数值策略

- 普通怪物卡片掉率从 1% 调整为 5%，属于本版本的数值策略调整。

### 验证

- 完整 adapter self-test：通过。
- managed `net8.0` build：0 error、0 warning。
- managed `net6.0/win-x64` self-contained publish：通过；SDK 仅提示 .NET 6 已结束支持周期。
- 原生 adapter 正式入口与 testports：确定性重建通过。
- 商城／整容专项：6/6 通过。
- 原生 UI 协议回归：4/4 通过。
- 全量 Python 测试：91 项通过，6 项因 source-only 条件跳过。
- GUI 布局、固定启动模式和称号读写自测：通过。
- 原客户端完整试玩仍由发布版验证，不把 host/self-test 表述为端到端验收。
