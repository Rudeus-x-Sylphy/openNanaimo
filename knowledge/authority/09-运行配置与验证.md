# 09 运行配置与验证

本主题只维护当前仓库发布候选的运行结构、产物身份、验证、回滚和 acceptance 边界。协议语义见01–07，静态地址见08。

## 当前运行结构

| 组件 | 仓库相对路径 |
|---|---|
| 一键入口 | `start_nanaimo_launcher.bat` |
| GUI | `gui_launcher/nanaimo_launcher.ps1` |
| 客户端连接器 | `gui_launcher/client_connect.ps1` |
| 完整 adapter | `adapter_runtime/Nanaimo.Adapter.exe` |
| 地宫桥接 | `adapter_runtime/nanaimo_gameplay_bridge.exe` |
| runtime 清单 | `adapter_runtime/adapter_manifest.json` |
| 活动发布清单 | `manifest/open_release_manifest.json` |
| 源码闭包 | `manifest/source_closure.json` |
| 导出候选清单 | `manifest/patch_allowlist.json` |

`manifest/open_release_manifest.json` 是唯一活动发布事实。仓库不保留第二份带独立哈希和发布状态的 current contract。

## 仓库候选身份

候选基线为当前工作树的地宫手动结算、资源权威与超级 Boss 评分持久化发布。运行包为 `net6.0/win-x64/self-contained`。`adapter_manifest.json` 登记256个 runtime 文件；目录另含清单本身。

| 文件 | Size | SHA-256 |
|---|---:|---|
| `Nanaimo.Adapter.exe` | 151040 | `13B328F061EB03B8026C4D61A745CB972FABA8A8921FFBDC5F0691FDD26D0DA5` |
| `Nanaimo.Adapter.dll` | 282624 | `D091E03FA394D507BCCFE0C7080331146FA282A8FE702383BB4566136DABED7C` |
| `Nanaimo.Gameplay.dll` | 4313088 | `3CCEBA65ADD99FC64F456F3A40BEAB67ACFA36C16C59529D86C262BEBCAB3D04` |
| `nanaimo_gameplay_bridge.exe` | 18296320 | `B3AF3FB214BF39D03AA24B9DC4D6C2A75D157A44B9AA2398A796E553450F9CED` |

确定性原生产物：

| 文件 | Size | SHA-256 |
|---|---:|---|
| `nanaimo_adapter_full.exe` | 18288128 | `DBA04CBB494DABB3BDA369EA209E7DF485DECCD7DA109E386E1288BCD737DAAF` |
| `nanaimo_adapter_testports_full.exe` | 18288128 | `A68B4C926144C4D0CC6B2935493D43636BC5C4D696AB969981410C769636B5EB` |
| `nanaimo_gameplay_bridge_full.exe` | 18296320 | `B3AF3FB214BF39D03AA24B9DC4D6C2A75D157A44B9AA2398A796E553450F9CED` |

当前合法客户端的兼容派生只作本机部署身份，不是全局哈希门禁：输入 `game.exe` size 14198272、SHA-256 `E7A6701EEC949F7655D274F0CF2D07F6511CBF8C569BEF6F98F66A2080A5A989`；派生输出 size 14198272、SHA-256 `86BABD83767FE20B7ADD093927295A61042CE2908B9503A7DA8B6B0B7C55D944`。派生保留家具 Index 与复活 HUD，新增普通结算双自动路径关闭、手动确认及连续关卡 P 弹恢复，20项复核全部通过。客户端和派生产物不进入仓库或导出包。

## 2026年9月25日当前候选

当前候选统一以下状态：

- 普通结算关闭 timer 与 action-state 两条自动 CF8B 路径，鼠标按钮调用点保留；
- 下一关链消费附带 CF73/CF1D，不再生成返城 CF1E/C368；主动回城保持独立握手；
- CF8B/CF8C 推进 managed 有效 tuple，超级 Boss stage1 的 B/A/S 与分数写入持久 slot3；
- 地宫 CF71/CF72、回城 C368/D8FF 使用角色配置最大 HP/MP，worker 当前值按该上限钳制；
- 启动器移除当前 HP/MP 输入，现有角色重启保留运行状态；
- 多模式 Boss 的 D012 总血条按完整 profile 剩余 HP 连续递减，最终组件 D012 保留实际 child/ordinal。

上述机制完成源码、host、确定性原生构建和客户端派生验证。
## 当前验证结果

- battle resource snapshot：25项检查通过；
- settlement/ranking transition：51项检查通过；
- town/apartment/death recovery：38项检查通过；
- split Boss：2个 Python 方法通过，覆盖4个原生场景与339个分体 Boss mode；
- split monster lifecycle：7/7通过；
- multistage Boss transition：2/2通过；
- death/clear rating policy：2/2通过；
- client compatibility derivation：11/11单元测试、当前客户端20/20派生复核通过；
- Python full suite：129项，其中124通过、5项因宿主机链接能力限制跳过；
- managed `net8.0` 与 `net6.0` Release 构建：0 error；net6只有既有停止支持警告；
- 正式、testports 与 bridge 原生输出完成独立确定性重建；
- GUI LaunchModes、Layout、TitleIO、CatalogPreview：全部通过；
- source closure：181文件，无 source/include gap；
- source-only 与完整 package、launcher Validate、public privacy、提交信息和 `git diff --check`：作为最终提交门禁执行；
- patch allowlist：223个候选，`missing=0`、dry-run `change_count=0`，全部仍为 review required；未自动扩大再分发批准。

这些结果证明源码闭包、adapter／客户端派生构造和仓库候选一致性，不证明当前候选的原客户端可见效果。

## 状态机制契约

- 02拥有正常回城、死亡返城及城镇／公寓资源恢复；
- 04拥有配置最大 HP/MP、battle epoch、结算冻结和 P 弹恢复；
- 06拥有普通、runtime/deform、crate、Boss 组件账本及多阶段终结；
- 07拥有普通／最终结算等待、评分、下一关授权和死亡离场；
- 08只维护对应静态地址链。

不得把 host/self-test 写成客户端已接受，也不得因单项试玩通过而自动提升整个发布。

## 回滚与部署边界

当前仓库 `adapter_runtime/` 已更新。替换前 runtime 保存在本工作树忽略的 `build/adapter-runtime-before-dungeon-control-20260925/`。

当前候选已发布到使用者本地根目录：项目拥有目录、runtime、启动器与客户端兼容派生均已更新，adapter 已按新 runtime 重启。发布前的项目、客户端、状态库和 profile 快照位于 `deployment_backups/dungeon-control-before-20260925`；回滚时应恢复同一快照中的代码、客户端和状态。

## Acceptance

`runtime_acceptance=false`。部署后至少需要原客户端验证：

1. 普通第一／第二／第三关结算页无操作时不自动推进，手动下一关仍可进入；第三关只在点击后进入超级 Boss；
2. 连续关卡和超级 Boss 入口保持实际 P 弹形态，重新从城镇开局不继承旧阶段；
3. 最大 HP/MP 在城镇、CF71、CF72、C368 和 D8FF 保持配置值；非满当前值跨结算和下一关连续；
4. 分体 Boss 逐个集中击杀时只退休当前组件，过量伤害不跨组件，全部必要组件结束后才结算；交替攻击的可见身体对应另行观察；
5. 死亡结算不显示成功 S、不增加成功进度；第一次点击回城即完成 CF74/CF1E 和 DeathReturn，不再卡死；
6. 正常回城与死亡回城初值正确，随后才按定时节奏恢复；
7. 地宫1及至少一个三阶段 Boss 的中间阶段终结和最终单次结算保持正确。
