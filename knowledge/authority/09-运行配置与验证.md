# 09 运行配置与验证

本主题说明当前完整 adapter 的文件结构、启动方式、构建身份和验证边界。协议字段与静态调用链见01–08；客户端兼容步骤见 [完整 adapter 与客户端兼容说明](../../docs/完整适配器与客户端兼容说明.md)。

## 当前运行结构

以下路径均相对运行根目录：

| 组件 | 路径 |
|---|---|
| 一键入口 | `start_nanaimo_launcher.bat` |
| GUI | `gui_launcher/nanaimo_launcher.ps1` |
| 客户端连接器 | `gui_launcher/client_connect.ps1` |
| 完整 adapter | `adapter_runtime/Nanaimo.Adapter.exe` |
| 地宫桥接 | `adapter_runtime/nanaimo_gameplay_bridge.exe` |
| adapter 文件清单 | `adapter_runtime/adapter_manifest.json` |
| 运行状态 | `adapter_data/` |
| 运行日志 | `adapter_data/logs/` |
| 完整 adapter 构建入口 | `scripts/build_complete_adapter.ps1` |
| 原生 adapter 构建入口 | `scripts/build_adapter.ps1` |
| 源码清单 | `manifest/source_closure.json` |

GUI 只管理一套完整 adapter。绿色“一键进入 Nanaimo”是主入口；蓝色按钮只单独管理同一套完整 adapter。

## 当前构建身份

The repository and playable parent runtimes were republished on September 25, 2026 after an attributable client run disproved the first ready-room-rank publication. The playable `retained-native-dungeon` route, not the managed CF71 builder, had still sent fixed `+0xB4=1` while the selected stage database rating was zero. The current build tracks the CF6C tuple, rewrites native-worker CF71 from the persisted member ledger at actual egress, zeros the high byte, recomputes checksum, and leaves the native fallback blank. CF15/CF16 leaderboard behavior is unchanged. Focused rank tests and full adapter self-test, including the real CF6C->CF70->CF71 route, pass; fresh visible original-client acceptance is pending.

完整 adapter 当前采用自包含 `net6.0/win-x64` 构建：

```text
Nanaimo.Adapter.exe
size   151040
sha256 5259B65167DAAA94C43FBF8AF549AB1EA777F6967E416EACDC60F7D854E71C04

Nanaimo.Adapter.dll
size   246784
sha256 E8EBC97BE489745923B1150D2A226A182DBA61A7D6401FFCDE3F6FB2759CC0D4

Nanaimo.Gameplay.dll
size   4261376
sha256 288499FB95EF0CF13AFFFF35C402B69CD7F7B35928D750C8CC59F9DD8489D804

nanaimo_gameplay_bridge.exe
size   18293760
sha256 10EA3F9EA700F1D8F5116A55EB99DF9E62DB924E9B261E56702F6C511ADC51B2
```

原生入口确定性重建：

```text
nanaimo_adapter.exe
size   18286080
sha256 58672D90AE49816398D76FD347AB621828B09ACD399FBED91AD68CF0AA9DE1B5

nanaimo_adapter_testports.exe
size   18286080
sha256 8C20CB4C827915C730BC03ACE4D713EB0283410E784240B790DC030381353113
```

源码清单记录原生 include、managed C# 构建输入和 reviewed 原生输出。当前功能包含任务、社交、交易、竞技、娱乐、公寓及相关 managed 数据层；普通怪物卡片掉率为 5%。

## 启动和连接

GUI 固定使用本机回环：

```text
11005 登录
11999 角色资料注册
12050 世界入口
```

一键启动顺序为：停止当前运行的 adapter 与客户端 -> 保存资料 -> 校验 adapter 文件闭包并确认 `game.exe` 存在 -> 从使用者自备文件派生并验证家具五字节 Index 重定向、第五村道路及 SSTG/PON 兼容资源 -> 启动完整 adapter -> 等待三个入口 -> 注册资料 -> 启动客户端。启动器保持 emotion EXE 代码不变；表情翻页由有界 C355 情侣姓名／戒指协议保障。C393 提供有界场景快照，本地 getter 负责家具点击路径。客户端整体哈希用于诊断；家具站点按 PE 布局和当前五字节精确匹配。桥接端口供完整 adapter 内部使用，GUI 固定使用 Network 模式，Standalone 构造器用于协议研究。

## 状态一致性

- 等级和经验、货币、库存、任务、宠物及关卡进度由完整 adapter 的持久层保存。
- 进入地宫时，managed 状态通过固定5120字节结构导入桥接组件；离开或结算时按差量事务写回。
- 宠物阶段／等级／经验在 C44C、C379、CF72 和 CF88 间保持同源。
- Hans 与 NaNa 商品按目录币种扣款；C3CD头像购买从`ava._D1`字段9取得Cash价格，只扣NaNa/Cash，C3CE在+0x10回写NaNa、+0x18回写Hans；整容券按准确库存 identity 消耗。
- Revival uses are restored through C355/local CF71. The ready-room card separately draws actor `+0x8124`; the deployed client now rewrites that text from manager `+0x5600` before each original HUD draw. 复活次数通过C355／本地CF71恢复。native dungeon CF83/12同时存在mode1复活次数路径和mode0 Hans路径。首轮mode1通过worker内部CF95推进并发送CF84 variant60；首次恢复后的第二次死亡帧为mode0/value950，现由managed扣Hans、内部F104/F105同步worker HP/MP并发送CF84 variant20。两者都以本地UID D010死亡锁为前置、不依赖0x044C；mode1额外要求`uses>0`。CF84 `+0x0C/+0x0E`携带HP/MP，`+0x10/+0x14`为零辅助字段。
- 同一提交ID不重复结算角色经验、宠物经验、货币、卡片或道具。

用户在2026年9月23日提供的原客户端日志重现了发布版进入村庄后 `FirstVillageFlag==TRUE -> CVillageChar(-1,-1)`。静态链证明日志中的 `1081671680` 对应 double 400.0，C368 坐标归一化已经到达客户端；失败来自 C355 提前恢复任意页面后，首次村庄分支改读缺失的页面入口。当前运行包已改为 C355 `map/page=0/0` 安全启动，并取消首个 C367 的存档页面覆盖。以下验证仍是静态／adapter 构造，不是修复候选的原客户端端到端验收。

## 当前验证结果

| 检查 | 结果 |
|---|---|
| Ready-room rank and settlement leaderboard | PASS (server construction): actual retained-native CF6C->CF70->CF71 egress normalizes +0xB4 blank/B/A/S; CF16 10x24 ordering/persistence remains passing. September25 user screenshot/runtime disproved the previous publication; fresh post-publication visible acceptance is pending |
| 完整 adapter self-test | PASS；隔离端口／临时资料运行到 `MIGRATION_CHECKS_PASS`，包含 C379 Z/X 技能格、库存、宠物、房间、地宫与事务回归 |
| managed Release build | PASS；net8目标0 warning／0 error，net6 self-contained仅SDK生命周期警告 |
| 仓库及实际父目录部署运行包 | PASS；EXE `5259B65167DAAA94C43FBF8AF549AB1EA777F6967E416EACDC60F7D854E71C04`，Adapter DLL `E8EBC97BE489745923B1150D2A226A182DBA61A7D6401FFCDE3F6FB2759CC0D4`，Gameplay DLL `288499FB95EF0CF13AFFFF35C402B69CD7F7B35928D750C8CC59F9DD8489D804`，bridge `10EA3F9EA700F1D8F5116A55EB99DF9E62DB924E9B261E56702F6C511ADC51B2` |
| 准备房“查看学分”评分板 | PASS；普通60字节评分、秘密4字节评分、未评分零值、最佳成绩不降级、C355访问归一化不覆盖评分区及数据库重开均通过构造检查；原客户端可见验收待完成 |
| C379 物品栏 Z/X 技能格 | PASS；两槽等级／代码、快捷栏与技能槽期限分离、未学习选择清零均通过构造检查；2026年9月24日用户验收确认肉弹型／炮弹型 Z/X 技能在重新打开物品栏后可见 |
| 继承的其他专项（本轮未重跑，不提升当前验收） | NaNa头像事务／C3CE余额顺序 `SHOP_NANA_AVATAR_CASH_PASS`，以及既有`pet-revival-village`、含C355安全启动回归的`village-position`、`channel-reentry`、部署版launcher Validate 均PASS |
| 原生正式／testports 确定性构建 | PASS |
| 商城／整容专项 | 7/7 PASS；新增managed PET/SP币种、四种付款模式、余额不足拒绝、C473分页/领取/300重复实例回归通过（证据目录shop-currency-pagination-20260924） |
| 原生 UI 协议回归 | 4/4 PASS |
| Python full suite | 114 tests: 109 PASS and 5 environment/external-dependency skips |
| GUI 布局／固定模式／称号读写 | PASS |
| Ready-room revival HUD derivation/deployment | Site gates, trampoline bytes, 9 derivation tests, repeated apply, parent script/launcher hash equality and launcher Validate PASS; client `4D938DE1...FEEC3` deployed; September 24 user run observed the displayed count matching the owned count. Screenshot and one-use decrement correlation remain unretained |
| source-only package structure | Native/managed source closure has175 files with zero source/include gaps. The verifier excludes the private `knowledge/evidence/` tree from the distributable public-text surface; source-only structure and public-text checks PASS. No runtime artifact acceptance is inferred from that verifier |

## 证据边界

本轮 C379 技能格问题可表述为：**客户端消费字段、角色技能账本和 adapter 构造已经闭合，仓库运行包与实际部署均已更新；2026年9月24日用户验收确认肉弹型／炮弹型 Z/X 技能在重新打开物品栏后可见。**

本轮商城问题可表述为：**分页静态链及币种源码差异已确认，adapter构造和持久化验证通过，仓库运行包及父目录实际部署均已更新、适配器已重启。** 不自动补偿历史误扣金币：缺少可归属交易凭据，不能猜测补偿金额。

仍不能用 host/self-test 代替原客户端试玩。下列项目保留运行验收：

- 宠物页等级、跨阶段提示与多人隔离；
- 新PET/特殊宝石扣款、14格待领取分页/第二页领取与重登；NaNa头像购买后的即时余额／物品显示、重进和重登持久性；整容后的即时动画、重进和重登显示；
- Post-patch ready-room bottom-right revival count must match manager/CF71 and no longer remain00; 原客户端首轮CF83 mode1、次数33→32与HP／操作恢复，以及首次恢复后第二轮CF83 mode0/value950对应的Hans扣减、CF84 variant20、再次可控恢复和后续结算；
- 家具选择的完整可见流程；
- 拉米诺斯 C355 载体的原客户端移动；地宫7道路、房间界面、三难度战斗、返回与再次进入；新进程 C355 `0/0` 安全启动候选下角色不再进入 `(-1,-1)`、随后可移动并持久化合法坐标；原始客户端文件上的 `+0xDF` 表情翻页边界；
- 好友外部桥、完整竞技／娱乐模式以及全部多人交易边界。

发现运行问题时，可记录客户端哈希作为诊断指纹（不得作为支持门禁），并记录 adapter manifest、角色、连接、操作时间、首个异常请求／响应和关闭方向，再回到对应主题修正，不应通过主动推送或固定返回值绕过本地状态门控。

## ???????????

??????????????? C365/C366 ????????? adapter build/self-test???? `--launcher-profile-self-test`?`--village-position-self-test`?GUI `-SelfTestTitleIO` ? `-ValidateOnly` ??????????????????????? `artifact-hashes.tsv`?`runtime_acceptance` ?? false?

????????????????? grade ???????????????????1?2?3?4?3??? C365/C366/C367/C368/CB21???3?4????? canonical page0 ???????
