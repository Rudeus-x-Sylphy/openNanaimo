# 构建与验证

以下命令均从仓库根目录执行。仓库不附带 Python、TinyCC、.NET SDK 或 Nanaimo 客户端；工具可通过脚本参数指定为仓库外路径。

## 1. Source closure 与 package

```powershell
python -B scripts/refresh_source_manifest.py
python -B scripts/verify_package.py --source-only
python -B scripts/verify_package.py
```

当前源码闭包为181文件。`adapter_manifest.json` 登记256个 runtime 文件，目录另含清单自身；`open_release_manifest.json` 登记267个 critical files。验证器核对大小、SHA-256、递归 include、活动 runtime 清单和公开文本；`runtime_acceptance=false` 不因 package 验证而改变。

完整 package 需要使用者自备的 `game.exe` 只作存在性检查，不对客户端大小或哈希设门禁。

## 2. 完整 Python 回归

```powershell
python -B -m unittest discover -s scripts -p 'test_*.py' -v
```

当前结果：129项，其中124通过、5项因宿主机链接能力限制跳过。

## 3. 地宫专项

```powershell
dotnet run --project managed-host/Nanaimo.Adapter.csproj -c Release -f net8.0 -- --battle-resource-snapshot-self-test
dotnet run --project managed-host/Nanaimo.Adapter.csproj -c Release -f net8.0 --no-build -- --dungeon-ranking-self-test
dotnet run --project managed-host/Nanaimo.Adapter.csproj -c Release -f net8.0 --no-build -- --health-recovery-self-test
python -B scripts/test_split_boss_component_lifecycle.py
python -B scripts/test_split_monster_lifecycle.py
python -B scripts/test_multistage_boss_transition.py
python -B scripts/test_dungeon_settlement_policy.py
python -B scripts/test_prepare_client_compatibility.py
```

当前结果：资源快照25项、结算／转移49项、非战斗恢复38项；分体 Boss 2个方法覆盖4个原生场景，普通分体怪物7/7，多阶段 Boss 2/2，死亡／成功评分2/2，客户端兼容单元测试11/11。专项证明构造和派生字节，不替代原客户端试玩。

## 4. Managed 构建

```powershell
dotnet build managed-host/Nanaimo.Adapter.csproj -c Release -f net8.0 --nologo
dotnet build managed-host/Nanaimo.Adapter.csproj -c Release -f net6.0 --nologo --no-restore
```

两个目标均为0 error；net6只有既有停止支持警告。

## 5. 完整 adapter 构建

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build_complete_adapter.ps1 `
  -OutputRoot '<全新候选目录>' `
  -TccPath '<TCC目录>/tcc.exe' `
  -DotnetPath '<.NET 8 SDK目录>/dotnet.exe' `
  -ResourceDataRoot '<adapter数据目录>' `
  -SelfTest
```

输出为 `net6.0/win-x64/self-contained`，包含 `Nanaimo.Adapter.exe`、managed DLL、`nanaimo_gameplay_bridge.exe`、资源数据及 `adapter_manifest.json`。必须使用不存在的新目录，避免旧 DLL 混入清单。

## 6. 原生确定性构建

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build_adapter.ps1 `
  -TccPath '<TCC目录>/tcc.exe' -KeepOutputs
```

bridge 采用相同 include 根独立构建两次并逐字节比较。当前 reviewed outputs：

| 文件 | Size | SHA-256 |
|---|---:|---|
| `nanaimo_adapter_full.exe` | 18287616 | `BDDC36449D803CE6C5EDB0FBEBB6CD3BDBE6018EED4D2B49EA390C2989C571A3` |
| `nanaimo_adapter_testports_full.exe` | 18287616 | `C27A5FA6CD4C0CB65A2EA69C42D51D6CCB0860B303BD118F13A2AA9CCBD9F3F5` |
| `nanaimo_gameplay_bridge_full.exe` | 18295808 | `1D2089E08715CD9C3D698BE549B36BDF0A43B5A65864B2355896578DF00A5523` |

## 7. 客户端兼容派生

```powershell
python -B scripts/prepare_client_compatibility.py `
  --source-root '<客户端根目录>' `
  --output-root '<全新派生目录>' `
  --furniture --revival-display --dungeon-state --dungeon7 --overwrite
```

当前合法客户端的独立派生通过19项复核：保留家具 Index、准备房复活 HUD、道路和别名，并加入普通结算手动确认与连续关卡 P 弹恢复。派生前后的 `game.exe` 均为14198272字节；本机输入／输出 SHA-256 分别为 `4D938DE18745F5BB9867FAC2B65866D4ADB2E9AD38AE49A4FC6A27C304CFEEC3` 和 `E7A6701EEC949F7655D274F0CF2D07F6511CBF8C569BEF6F98F66A2080A5A989`。这些哈希只描述当前本机部署候选，不是全局客户端许可或支持门禁。

## 8. GUI 自测与 Validate

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -STA -File gui_launcher/nanaimo_launcher.ps1 -SelfTestLaunchModes
powershell -NoProfile -ExecutionPolicy Bypass -STA -File gui_launcher/nanaimo_launcher.ps1 -SelfTestLayout
powershell -NoProfile -ExecutionPolicy Bypass -STA -File gui_launcher/nanaimo_launcher.ps1 -SelfTestTitleIO
powershell -NoProfile -ExecutionPolicy Bypass -STA -File gui_launcher/nanaimo_launcher.ps1 -SelfTestCatalogPreview
powershell -NoProfile -ExecutionPolicy Bypass -STA -File gui_launcher/nanaimo_launcher.ps1 -ValidateOnly
```

四项 GUI 自测不启动客户端，当前全部通过；Validate 使用当前候选 runtime 与使用者自备客户端执行，只验证文件／配置／网络入口，不启动游戏。

## 9. 发布清单

```powershell
python -B scripts/refresh_manifest.py
python -B scripts/refresh_patch_manifest.py --dry-run --summary
```

`open_release_manifest.json` 是唯一活动发布事实。`patch_allowlist.json` 当前包含223个候选文件，`missing=0`、`change_count=0`；所有条目仍为 review required。候选清单不是再分发批准，刷新哈希不得自动扩张授权范围。

## 10. 发布前检查

```powershell
git diff --check
python -B scripts/verify_package.py --source-only
python -B scripts/verify_package.py
```

还必须运行 public privacy 与提交信息扫描，确认没有私有路径、受限来源或操作过程；清除损坏问号段落和竞争清单。最终报告区分“静态已证实”“adapter／派生构造验证”“仓库候选验证”和“原客户端运行观察”。2026年9月25日上一候选的负面试玩只用于撤回旧主张，不能计作当前候选通过。